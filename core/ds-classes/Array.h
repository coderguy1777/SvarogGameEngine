#pragma once
#ifndef ARRAY_H
#include "macrodefs.h"
#include "ArrayList.h"
template <class A>
class Array {
    private: 
        ArrayList<A>Array;
    public: 
        Array(int itema) {
            Array.add(itema);
        }

        A drainitems() {
            for(unsigned int i = 0; i < )
        }

};

#endif
